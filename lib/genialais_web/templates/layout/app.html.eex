<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Genialais · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Tangerine&display=swap" rel="stylesheet"> 
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <%= if Pow.Plug.current_user(@conn) do %>
            <%= form_for @conn, Routes.user_path(@conn, :update, %{}), fn f -> %>
              <%= select f, :locale, languages(), selected: Pow.Plug.current_user(@conn).locale, class: "locale-select", onchange: "this.form.submit()" %>
              <%= hidden_input f, :redirect_to, value: @conn.request_path %>
            <% end %>
            <%= link "Sign out", to: Routes.pow_session_path(@conn, :delete), method: :delete %>
          <% else %>
            <%= link "Sign in", to: Routes.pow_session_path(@conn, :new) %>
          <% end %>
        </nav>
        <a href="/">
          <span class="app-title">Genialais</span>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
